<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    tools:context="pi.restaurantapp.ui.fragments.management.restaurantdata.EditRestaurantData">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        app:layout_behavior="@string/appbar_scrolling_view_behavior">

        <include
            android:id="@+id/cardAboutRestaurant"
            layout="@layout/card_view_wide" />

        <include
            android:id="@+id/cardOpeningHours"
            layout="@layout/card_view_wide" />

        <include
            android:id="@+id/cardLocation"
            layout="@layout/card_view_wide" />

        <include
            android:id="@+id/cardDelivery"
            layout="@layout/card_view_wide" />

    </LinearLayout>

</androidx.coordinatorlayout.widget.CoordinatorLayout>